# AGENTS.md

## 语言与沟通

- 默认使用中文回复。
- 回答要直接、具体，不要长篇解释。
- 修改代码前，先简要说明计划。
- 修改完成后，必须总结改了什么、如何验证、还有什么风险。

## 项目目标

本项目目标是：

- 稳定推进功能开发
- 清晰记录项目进度
- 每次修改都能追踪原因、结果和下一步

## 重要文件

请优先阅读这些文件：

- `README.md`：项目说明
- `PROJECT_PROGRESS.md`：项目进度
- `TODO.md`：待办任务
- `ROADMAP.md`：路线图
- `CHANGELOG.md`：版本变化
- `docs/`：项目文档

如果这些文件不存在，可以在需要时建议创建。

## 工作流程

每次处理任务时，请按以下流程：

1. 先理解用户需求。
2. 检查相关文件和当前代码状态。
3. 给出简短执行计划。
4. 修改代码或文档。
5. 运行必要的检查。
6. 更新 `PROJECT_PROGRESS.md`。
7. 最后总结：
   - 本次完成了什么
   - 修改了哪些文件
   - 是否通过测试
   - 下一步建议做什么

## 资产审核通过后的处理规则

当 `06_待审核内容/` 中的资产设定文件经过甲方审核通过后，**必须自动执行以下流程**：

1. **移动文件**：将审核通过的资产文件从 `06_待审核内容/` 移动到对应的资产文件夹中：
   - 角色 → `02_角色/<角色名>/`
   - 场景 → `03_场景/<场景名>/`
   - 道具 → `04_道具/<道具名>/`
   - 如果目标子文件夹不存在，则创建。
2. **从待审核中删除**：确认移动成功后，删除 `06_待审核内容/` 中的原文件。
3. **更新资产设定表**：在 `资产设定表.md` 中将该资产状态改为 `甲方审核通过`，并更新文件/位置列指向新路径。
4. **更新进度文档**：同步更新 `PROJECT_PROGRESS.md`、`TODO.md`、`GITHUB_任务看板.md`。
5. **关闭对应 GitHub Issue**：将该资产 Issue 的状态更新为已完成。

**禁止**：审核通过后遗留文件在 `06_待审核内容/` 中。`06_待审核内容/` 只应包含待审核或审核中的文件。

## 镜头状态自动升级规则

当某一段镜头所需要的**全部资产**都已在 `资产设定表.md` 中标记为 `甲方审核通过` / `已有` / `可复用` / `不单独制作`（即不再依赖任何未完成的资产）时，该段镜头的状态**必须自动从 `待开始` 升级为 `进行中`**。

判断依据：对照该段镜头涉及的资产 ID（CHAR-/SCN-/PROP-），逐一查 `资产设定表.md` 确认状态。全部满足则自动升级。

**禁止**：资产未全部到位的情况下手动将镜头标为 `进行中`。

## 项目进度管理规则

每次完成任务后，都要更新 `PROJECT_PROGRESS.md`。

格式如下：

```md
# Project Progress

## 当前状态

简要说明项目当前做到哪里。

## 最近完成

- YYYY-MM-DD：完成内容

## 进行中

- 当前正在处理的任务

## 待办事项

- [ ] 待办任务 1
- [ ] 待办任务 2

## 阻塞问题

- 暂无 / 具体问题

## 下一步

- 下一步最优先要做的事情
```
