# GitHub 看板维护方案

## 目标

以后不把 GitHub Project Board 当作主数据源维护。  
项目任务以 GitHub Issue 为准，Project Board 只负责自动展示 Issue 的当前状态。

这样可以避免 Codex 反复直接拖动看板，也避免本地文档、Issue、Project Board 三处状态不一致。

## 核心规则

1. **任务只维护 Issue**
   - 需要人执行、等待、审核、返修的事项，建立或更新 Issue。
   - 不需要制作的资产不建 Issue，只记录在资产表。
   - 甲方原始资料不上传 GitHub，Issue 中只写本地资料位置或摘要。

2. **Project Board 自动展示 Issue**
   - 新 Issue 自动进入 Project。
   - Issue 状态变化后，由 Project Workflow 或 Issue 字段同步到对应列。
   - Codex 原则上不直接拖 Board 卡片，除非是一次性修正错误状态。

3. **本地文档仍是制作主控**
   - `PROJECT_PROGRESS.md` 记录项目总体状态。
   - `项目进度总表.md` 记录集数级进度。
   - 每集镜头制作进度表记录镜头级状态。
   - `资产设定表.md` 记录角色、场景、道具设定与审核状态。

## Issue 命名规范

Issue 标题统一使用以下前缀：

| 前缀 | 用途 |
|---|---|
| `【剧本】` | 剧本整理、剧本转提示词 |
| `【提示词】` | 视频生成提示词拆解、调整 |
| `【镜头】` | 镜头生成、镜头补做 |
| `【反馈】` | 甲方反馈收集、反馈整理 |
| `【修改】` | 反馈后返修任务 |
| `【资产】` | 需要制作、补齐、替换或等待的资产 |
| `【审核】` | 需要甲方审核或确认的问题 |
| `【后期】` | 剪辑、合成、替换素材、输出版本 |
| `【流程】` | 项目管理、同步、交接、规则调整 |

示例：

```text
【审核】第8集一审待甲方确认问题同步
【资产】制作旧玩具单体资产
【修改】第8集反馈后修改版镜头生成
【后期】第8集修改版剪辑合成
```

## 推荐标签

| 标签 | 用途 |
|---|---|
| `第8集` / `第9集` | 集数筛选 |
| `剧本` | 剧本与提示词前置工作 |
| `提示词` | 提示词生成与调整 |
| `镜头生成` | 镜头生成任务 |
| `资产` | 资产制作、资产等待、资产替换 |
| `甲方反馈` | 反馈整理、反馈同步 |
| `甲方审核` | 需要甲方审核通过 |
| `待确认` | 需要甲方确认问题 |
| `后期` | 剪辑合成相关 |
| `阻塞` | 当前无法继续推进 |
| `不上传素材` | 涉及甲方原始资料，仅记录摘要 |

## Project Board 状态列

建议保留并使用以下状态列：

| 状态列 | 含义 | 进入条件 | 离开条件 |
|---|---|---|---|
| `待开始` | 未开始 | 新建任务，暂无阻塞 | 有人开始执行或进入等待/审核状态 |
| `进行中` | 制作中 | 正在写提示词、生成镜头、整理反馈或制作资产 | 完成、等待甲方或进入返修 |
| `待甲方提供` | 等甲方给资料 | 缺少甲方提供的素材、资产或信息 | 甲方资料到位 |
| `待甲方审核` | 等甲方审核 | 我方已提交资产、成片或问题表 | 甲方通过或提出修改 |
| `待确认` | 等甲方确认具体问题 | 需要甲方选择、裁定或补充说明 | 收到明确答复 |
| `修改中` | 反馈后返修 | 已确认需要改，正在返修 | 返修完成并提交审核 |
| `已完成` | 完成 | Issue 已关闭或任务明确完成 | 不再移动 |
| `归档` | 历史任务 | 已完成且不需要日常查看 | 不再移动 |

当前 GitHub Project 已统一为中文列名：

- `待开始`：未开始的实际制作任务。
- `进行中`：正在执行的任务。
- `待审核`：等待甲方提供、审核或确认的任务。
- `修改中`：反馈后返修任务。
- `已完成`：已完成或已关闭的任务。
- `归档`：历史任务。

## 自动化规则

在 GitHub Project 的 Workflows 中配置：

1. **Item added to project**
   - 新 Issue 加入 Project 后，Status 设置为 `待开始`。

2. **Issue closed**
   - Issue 被关闭时，Status 设置为 `已完成`。

3. **Issue reopened**
   - Issue 重新打开时，Status 设置为 `待开始` 或 `进行中`。

4. **Label-based status**
   - 加 `待甲方提供` 标签，Status 设置为 `待甲方提供`。
   - 加 `甲方审核` 标签，Status 设置为 `待甲方审核`。
   - 加 `待确认` 标签，Status 设置为 `待确认`。
   - 加 `修改` 标签，Status 设置为 `修改中`。
   - 加 `阻塞` 标签，Status 设置为 `待确认` 或单独的 `Blocked` 列。

GitHub 原生 Project Workflow 对“按标签自动改 Status”的能力有限时，可以先用 GitHub Actions 实现；暂时无法配置 Actions 时，由 Codex 更新 Issue 后再做一次 Project 状态校验。

## 当前已验证的 GitHub Project 配置

2026-05-23 已在 GitHub Project Workflows 页面确认：

| Workflow | 当前状态 | 配置 |
|---|---|---|
| `Auto-add to project` | On | 过滤器为 `is:issue is:open`，新建或更新的打开 Issue 会自动加入 Project。 |
| `Item added to project` | On | 新加入 Project 的 issue / pull request 自动设置 `Status: 待开始`。 |
| `Item closed` | On | issue / pull request 关闭后自动设置 `Status: 已完成`。 |
| `Item reopened` | On | 重新打开的项目会自动回到待处理状态。 |

因此当前可稳定执行的自动化是：

```text
新建 Issue
↓
自动进入 Project
↓
自动进入 待开始
↓
关闭 Issue
↓
自动进入 已完成
```

`待甲方提供`、`待甲方审核`、`待确认`、`修改中` 这些更细状态，暂时采用两种方式维护：

1. 优先通过 Issue 标题、正文和标签表达真实状态。
2. 每次 Codex 更新 Issue 后，检查 Project Board，必要时再做一次 Project 状态校验。

等 GitHub App/API 写权限完整后，再考虑增加 GitHub Actions，把标签变化自动同步到 Project Status。

## 资产状态同步铁律（2026-05-24 增设）

**资产状态变化时，以下三处必须同时更新，缺一不可：**

| # | 位置 | 更新内容 |
|---|---|---|
| 1 | `资产设定表.md` | 资产行的"状态"列 |
| 2 | `GITHUB_任务看板.md` | 对应 Issue 行的"状态"和"标签"列 |
| 3 | GitHub Issue | 标签（label）更新 |
| 4 | GitHub Project Board | Status 列同步（通过标签变化 + API 校验） |

**资产状态 → 看板列的映射规则：**

| 资产表状态 | Issue 标签 | 看板列 |
|---|---|---|
| 待内部审核 / 待甲方审核 | `待审核` | 待审核 |
| 待制作 / 待制作/待甲方审核 | `进行中` | 进行中 |
| 甲方审核通过 | `已完成` | 已完成 |
| 不单独制作 / 可复用 / 已有 | 无 Issue | — |

**常见错误（禁止）：**

- 只更新资产表，忘记更新 Issue 标签和看板 → 造成 #78/#79 类不一致
- 手动拖看板卡片而不改 Issue 标签 → 下次同步时覆盖
- 标签用 `待确认` 代替 `待审核` → 混淆"等甲方答问题"和"等甲方审资产"

## Codex 维护流程

每次进度变化后，Codex 按这个顺序处理：

1. 更新本地制作文档。
2. 更新 `PROJECT_PROGRESS.md`。
3. 判断是否需要新建 Issue。
4. 对已有 Issue 更新标题、正文、标签、关闭状态。
5. **核对资产 Issue 标签与资产表状态是否一致（按上方映射表）。**
6. 让 Project Workflow 自动同步看板。
7. 检查 Project Board 是否与 Issue 状态一致。

### Git 同步铁